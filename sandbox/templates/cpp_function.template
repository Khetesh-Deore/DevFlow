#include <iostream>
#include <vector>
#include <string>
#include <sstream>
#include <algorithm>
using namespace std;

{{USER_CODE}}

int main() {
    string line;
    while (getline(cin, line)) {
        if (line.empty()) continue;
        
        {{INPUT_PARSER}}
        
        {{FUNCTION_CALL}}
        
        {{OUTPUT_FORMATTER}}
    }
    return 0;
}
